const apiBaseURL = process.env.API_HOST;
const authServicePath = `${apiBaseURL}/auth/api/v1`;
const storeServicePath = `${apiBaseURL}/store/api/v1`;

export { apiBaseURL, authServicePath, storeServicePath };